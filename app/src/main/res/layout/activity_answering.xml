<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/answeringMainLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:weightSum="1">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="248dp"
        android:id="@+id/exampleImage"
        android:src="@drawable/examplge_image_bg"
        android:layout_gravity="center_horizontal"
        android:layout_weight="0.45" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:text="test"
        android:textColor="#ff0000ff"
        android:textSize="22dp"
        android:textStyle="bold"
        android:layout_marginTop="50dp"
        android:layout_alignLeft="@id/exampleImage"
        android:layout_alignRight="@id/exampleImage"
        android:layout_alignTop="@id/exampleImage"
        android:gravity="center"
        android:visibility="invisible"
        />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:id="@+id/imageView"/>

</RelativeLayout>
